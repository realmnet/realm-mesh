# Gateway Database Configuration (Docker)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=gateway_db
DB_USER=gateway_user
DB_PASSWORD=gateway_pass
DATABASE_URL=postgresql://gateway_user:gateway_pass@localhost:5433/gateway_db

# Admin API Security
ADMIN_API_KEY=admin-key-123

# Internal Gateway
INTERNAL_PORT=8080

# External Gateway (for partners)
EXTERNAL_PORT=8443

# Admin Console API
ADMIN_PORT=3001

# Logging
LOG_LEVEL=info
NODE_ENV=development