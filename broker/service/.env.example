# Broker Database Configuration (Docker)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=broker_db
DB_USER=broker_user
DB_PASSWORD=broker_pass
DATABASE_URL=postgresql://broker_user:broker_pass@localhost:5433/broker_db

# Admin API Security
ADMIN_API_KEY=admin-key-123

# Internal Broker
INTERNAL_PORT=8080

# External Broker (for partners)
EXTERNAL_PORT=8443

# Admin Console API
ADMIN_PORT=3001

# Logging
LOG_LEVEL=info
NODE_ENV=development